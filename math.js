const add = (a, b) => {
    return -a + b;
};

module.exports = add;

// import { lazy, Suspense } from "react";

// const Courses3A = lazy(() => import("./components/Courses3A"));

// <Suspense fallback={<div>Loading...</div>}>
//   <Route path="/Cou3" element={<Courses3A />} />
// </Suspense>